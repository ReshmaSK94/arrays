/**
 * Find the count of smaller elements on right of each element in an array
 * @author reshma
 *
 */
public class CountSmallerArray {
	/*
	 * Time Complexity: O(n2)
	 * Space Complexity: O(1)
	 * 
	 * If we use balanced binary search tree [like AVL tree], then the 
	 * Time Complexity: O(nlogn)
	 * Space Complexity: O(n)
	 * */
}
